credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope:    SYSTEM
              id:       github-access
              username: rendevor
              password: ${jenkins_github_access}