credentials:
  system:
    domainCredentials:
      - credentials:
          - usernamePassword:
              scope:    GLOBAL
              id:       github-access
              username: rendevor
              password: ${jenkins_github_access}
